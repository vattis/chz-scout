# Local Development Profile
spring:
  # Database - PostgreSQL (pgvector 지원, 로컬은 SSL 비활성화)
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:chz_scout}?sslmode=disable
    username: ${DB_USERNAME:chzscout}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  # JPA - Local Settings
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # Redis - Local
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

# Server
server:
  port: 8080

# CORS
cors:
  allowed-origins: http://localhost:5173

# Logging - Debug level for local
logging:
  level:
    com.vatti.chzscout: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

discord:
  bot:
    token: ${DISCORD_BOT_TOKEN:your-bot-token}
  oauth:
    redirect-uri: http://localhost:5173/api/auth/callback

openai:
  api:
    key: ${OPENAI_API_KEY:your-api-key}
    model: gpt-5-nano

# Encryption (key from .env.local - DO NOT hardcode!)
encryption:
  key: ${ENCRYPTION_KEY}