# Test Profile (CI/CD용)
spring:
  # H2 In-memory Database
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  # JPA - Test Settings
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: h2

  # Redis - Disable for tests
  data:
    redis:
      host: localhost
      port: 6379

# Logging
logging:
  level:
    com.vatti.chzscout: DEBUG
    org.hibernate.SQL: DEBUG

# Encryption (dummy key for test)
encryption:
  key: test-encryption-key-12345

# External API Keys (dummy values for test)
chzzk:
  api:
    base-url: https://api.chzzk.naver.com
    client-id: test-client-id
    client-secret: test-client-secret

discord:
  bot:
    token: test-bot-token

openai:
  api:
    key: test-api-key
    model: gpt-5-nano
    base-url: https://api.openai.com

# Embedding Settings (테스트용)
embedding:
  model: text-embedding-3-small
  dimensions: 1536
  batch-size: 100